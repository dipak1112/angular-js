window.App = angular.module('AngularCasts', ['ngResource'])

# we create a new module called AngularCasts and assign it to window.App.
# We also add the dependency of ngResource which provides simple REST client functionality.
